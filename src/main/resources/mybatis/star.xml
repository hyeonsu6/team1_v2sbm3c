<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dev.mvc.star.StarDAOInter">
	<!-- 등록 -->
	<insert id="create" parameterType="dev.mvc.star.StarVO">
		INSERT INTO star (starno, star, contentsno, memberno, rdate)
		VALUES(star_seq.nextval, #{star}, #{contentsno}, #{memberno}, sysdate)
	</insert>

</mapper>